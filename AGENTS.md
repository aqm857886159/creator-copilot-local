# 内容创作助手：Agent 工程总规则

这是本项目的唯一根级 Agent 规则文件。`CLAUDE.md` 和 `CODEX.md` 只作为兼容入口，不维护重复规则；进入任何子目录前，先读取最近的嵌套 `AGENTS.md`。

## 1. 项目身份

本项目是一个本地优先的内容创作操作系统，首个工作流是“抖音真人深度口播”，底层必须能扩展到访谈、测评、课程、播客切片和多平台内容。

核心闭环：

```text
账号/对标/热点 → 选题 → 脚本 → 分镜 → 拍摄任务
→ 素材库 → AI 剪辑提案 → 自有时间线 → 外部精修/导出
→ 发布 → 数据回流 → 创作记忆
```

当前仓库是 PRD、研究和早期界面 scaffold，不要把尚未实现的能力描述成已经存在。两个内部参考仓库可直接阅读和复用：

- `/Users/aoqimin/Desktop/e-cut`：内容研究、素材事实、编辑操作、确定性剪辑执行；
- `/Users/aoqimin/Desktop/Nomi`：Electron 本地运行时、Provider 目录、素材上传、异步任务和审批/恢复。

复用内部代码时保留许可证、版权和依赖边界；不要因为“参考仓库”而复制未经审计的密钥、用户数据或临时产物。

## 2. 事实源与阅读顺序

开始工作时按以下顺序建立上下文：

1. 读取本文件和目标路径最近的嵌套 `AGENTS.md`；
2. 读取用户当前任务和已拍板的决策；
3. 按任务读取必要文档，不要把所有研究文档都塞进上下文：
   - `docs/PRD-v0.1.md`：产品范围、用户和流程；
   - `docs/Research-and-Architecture-v0.1.md`：研究、架构和技术取舍；
   - `docs/Internal-Reuse-Audit-v0.1.md`：两个内部仓库的复用边界；
4. 修改现有代码前，先读真实调用方、测试和注释，确认现有行为为什么这样设计；
5. 涉及第三方 API、模型、桌面框架或交换格式时，先查当前官方文档，再查参考仓库的实际实现。

## 3. 不可破坏的产品与架构原则

- **本地优先**：账号、选题、脚本、分镜、素材索引、时间线、发布和复盘是本地事实；云端 Provider 只是可替换能力。
- **通用内核、工作流包**：口播是首个切入场景，不把领域模型写死为口播；差异放到 workflow/playbook 和策略配置。
- **A 方案拍摄**：软件生成逐镜头拍摄任务、示意图和文字指导；用户用手机或相机拍摄后导入。不要默认控制手机、相机或电脑摄像头。
- **AI 先提案、人可审阅**：AI 生成脚本、分镜、素材匹配和剪辑提案；破坏性操作必须可预览、确认、撤销和比较。
- **表达优先**：先理解创作者的真实表达、思考方式和边界，再生成文本；不以通用流畅度、随机口头禅或错别字伪造“人味”。
- **内部项目格式是事实源**：MP4、SRT/VTT、FCPXML、剪映草稿和交付包是输出适配器，不得反向成为账号、脚本和素材的主数据库。
- **确定性执行**：正式剪辑只接受 `FrozenEditSpec`，编译为 `ResolvedEditSpec` 和 `RenderIR`；渲染热路径不得临时调用 LLM/VLM 重新选材或改参数。
- **Provider 解耦**：公共 UI 和领域层只使用稳定的 `modelKey`、能力和中性参数；APIMart、TikHub、OpenAI、ComfyUI 等内部字段只存在于适配器。
- **可追溯**：每个 AI/Provider 决策都尽量保存来源、版本、素材 ID、参数摘要、状态变化和产物 hash。

## 4. 代码组织与边界

目标结构按职责拆分，禁止把所有逻辑堆进一个页面、路由或服务：

```text
apps/desktop          Electron 主进程、渲染器、preload、SQLite、任务调度
packages/domain       Creator/Research/Creation/Media/Editing/Publishing
packages/providers    Provider、Connector、模型目录、参数翻译、错误映射
packages/media        ffprobe/FFmpeg、代理、转写、OCR、镜头、索引
packages/agent-tools  UI/Agent/MCP 共用的类型化 Tool/Command
packages/exchange     RenderIR、MP4、FCPXML、剪映和交付包适配器
docs                  PRD、架构、计划、评测、审计和决策记录
```

约束：

- Electron renderer 不直接读文件、拿 API key、写数据库或调用第三方 API；这些通过 main/preload 的最小 IPC 边界完成。
- 领域规则只实现一次；不要在 UI、API、Agent 和渲染器分别复制同一规则。
- Provider 必须实现统一的 submit/poll/download/validate 状态和错误契约；不支持的能力要显式报错。
- Agent 只能调用注册过的结构化命令，不能直接写任意 JSON、剪映草稿或数据库。
- 新增跨模块概念前先确认应放在哪个包；单文件接近 500 行就优先拆模块，超过 800 行必须说明原因或拆分。

## 5. API、密钥与素材规则

- 本地 key 可以用于经过边界控制的联调，但不得输出、提交、写日志、写截图、写项目 JSON 或写测试 fixture。
- 主进程持有密钥，渲染器只拿到 `configured/hasCredential` 等非敏感状态。
- 真实 APIMart/TikHub 调用必须是有范围的：说明端点、测试素材、预计成本/额度、结果保存位置和清理方式；禁止后台循环、批量抓取或批量生成。
- 默认先用 mock/fixture 验证请求体、轮询、状态、错误和下载回收；真实联调不能代替合同测试。
- 临时公网素材 URL 必须记录过期时间并在本地导入；不能把供应商临时 URL 当成永久资产路径。
- 上传按媒体类型路由：图片、视频、音频必须验证供应商能力，禁止把视频送到 image-only 接口。
- 原始 Provider 响应只作为证据或调试材料保存，先脱敏、限体积并计算 hash；业务层使用归一化对象。

## 6. 修改流程

### 先找现成解法

遇到任何「这个该怎么做」的问题，默认假设**已经有人解决过**。先去找到他们怎么解决的，再决定要不要自己写。

- **查证顺序**：目标产品的源码 → 官方文档 → 同类开源实现 → 论文 → 二手总结。二手总结只用来找线索，不作为结论。
- **必须查清三件事**：机制（具体怎么实现的，不是名词）、许可证、能不能被闭源商业桌面应用采用。
- **区分抄代码和抄设计**：代码是受保护的表达，采用需要许可证；交互模型和架构思路不受保护，可独立重写。GPL/AGPL 项目只看不抄，且不要边读它源码边写我们的实现。
- **许可证必须读 LICENSE 原文**，不信 GitHub 标签。本项目已确认的坑：`etro` GPL-3.0、`videogrep` 反资本主义协议、`Remotion` 非开源且带域名黑名单、`Diffusion Studio Core` 无 key 打水印、`OpenChatCut` 原版 AGPL-3.0 无例外、MLT 的 `qt`/`plusgpl` 模块为 GPL。
- **只有确认没人解决过、或现成方案确实不适用，才自己写**，并在计划里写明为什么不适用。
- 结论写进 `docs/`，必须记录来源 URL、许可证、查证日期，以及「我们采用的是代码还是思路」。

反面模式：把「做个 spike 决定架构」当成第一步。如果这是个通用问题（播放、转码、时间戳、撤销、冲突合并、任务队列），它几乎一定已有标准解法；spike 应该用来**验证移植**，不是用来**重新发明**。

### 开始前

- 先确认当前目录、Git 根目录、分支和工作区状态；保护用户已有修改和未跟踪文件。
- 多文件或架构修改先在 `docs/plan/` 写范围、不动项、风险、回滚和验收门。
- 选型先查官方文档和当前源码；不要根据旧记忆猜模型端点、参数、输出格式或许可证。
- 用户可见 UI 改动先查看设计系统和真实现状，必要时先出可体验样张再实现。

### 实现时

- 优先小而完整的垂直切片，不做没有调用方和验证的“未来抽象”。
- 保持公共 API、项目文件和事件状态向后兼容；破坏性变化必须更新迁移、fixture 和文档。
- 新增行为同时补正常、失败、超时、重复提交和恢复路径；AI 输出必须有 schema、fallback 和可观察状态。
- 不顺手做全局重命名、格式化、依赖升级或 UI 重构；发现无关问题记录到合适的 issue/文档。

### 完成前

- 先跑最小相关测试，再跑 `typecheck`、`build` 或仓库规定的检查。
- 对用户可见功能，必须走一次真实用户任务；截图或导出物要与预期对账，不能只依赖断言。
- 检查 diff、秘钥模式、临时文件和文档链接；没有实际运行的命令不得声称运行过。
- 说明改了什么、验证了什么、剩余风险是什么；不要用“应该可以”代替证据。

## 7. 当前 scaffold 命令

以 `package.json` 为准，当前可用命令为：

```text
npm run typecheck
npm run build
npm test
```

`npm run dev` / `npm run start` 当前依赖 `server/index.ts`，在服务端 scaffold 完整前不要把它当作已验证的开发入口。新增或调整命令后，必须同步更新本节和 README。

## 8. 文档与规则维护

- 根 `AGENTS.md` 只写高频、全项目、可执行的规则；完整 API 说明、研究和历史放 `docs/`。
- 规则若只适用于 `apps/`、`server/`、`packages/` 或 `docs/`，放到对应目录的嵌套 `AGENTS.md`。
- 同一规则只维护一份；`CLAUDE.md`/`CODEX.md` 不复制正文。
- Agent 连续两次犯同一类错误，才把可复现的修复规则加入本文件；一次性教训进入审计/记忆文档。
- 架构、许可证、Provider、输出格式或用户流程改变时，必须更新对应 PRD/研究/决策文档。

## 9. 独立评审职责

- 对产品、架构和重大功能方案主动寻找反例、遗漏、失败路径、权限/成本风险和被误当成事实的假设；不要只顺着现有 PRD 补功能。
- 调研先看官方文档、源码和本项目真实测试，再看营销页或二手总结；结论明确分为事实、推断和建议，并保留来源与日期。
- 每次重大评审至少回答：真正的用户结果是什么、哪个假设最危险、已有项目已经验证了什么、哪些能力应后置、需要什么真实任务才能证明。
- e-cut 与 Nomi 是内部肩膀：优先复用其真实用户审查、状态机、合同测试、Provider 纪律和失败恢复，不重复做只存在于文档里的抽象层。
- 竞品功能不能直接成为需求；只有能改变用户决策、缩短真实任务或降低风险的模式才能进入 PRD。

## 10. 完成定义

一项工作只有同时满足以下条件才算完成：行为符合用户目标；边界和失败路径明确；相关测试或 fixture 通过；文档与事实一致；没有泄露密钥或临时数据；用户能看见可验证的结果。
