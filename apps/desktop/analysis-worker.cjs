// Utility-process compatibility entry. The implementation remains in the
// legacy path until its runtime imports and cancellation protocol are migrated.
require("../../electron/analysis-worker.cjs");
